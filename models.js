const Promise = require('promise');
const mongoose = require('mongoose');
const Schema = mongoose.Schema;

exports.difineModels = () => {
    var definedModels = new Object();

    return definedModels;
}